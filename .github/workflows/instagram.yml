name: Publication Instagram

on:
  # Déclencheur principal : appel externe (cron-job.org) sur workflow_dispatch
  # à 18h30 Europe/Paris précises, lundi et jeudi. Le schedule GitHub ci-dessous
  # n'est qu'un filet de sécurité : son horaire n'est jamais garanti par GitHub
  # (retards de plusieurs heures observés), mais publish.py est idempotent
  # (ne republie pas un post déjà "published"), donc un déclenchement en double
  # ne cause aucun doublon sur Instagram.
  schedule:
    - cron: "30 17 * * 1"
    - cron: "30 17 * * 4"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      PAGE_ACCESS_TOKEN: ${{ secrets.PAGE_ACCESS_TOKEN }}
      IG_ACCOUNT_ID: ${{ secrets.IG_ACCOUNT_ID }}
    steps:
      - name: Attendre le créneau 17h30 UTC si démarrage anticipé
        if: github.event_name == 'schedule'
        run: |
          TARGET=$(date -u -d "today 17:30" +%s)
          NOW=$(date -u +%s)
          WAIT=$((TARGET - NOW))
          if [ "$WAIT" -gt 0 ] && [ "$WAIT" -le 900 ]; then
            echo "Démarrage anticipé — attente de ${WAIT}s jusqu'à 17h30 UTC"
            sleep "$WAIT"
          fi
          echo "Heure de lancement : $(date -u '+%H:%M:%S UTC') = $(TZ=Europe/Paris date '+%H:%M:%S %Z')"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Publier le post du jour
        run: python pipeline.py --publish

      - name: Sauvegarder l'état des posts
        run: |
          if ! git diff --quiet -- captions.json; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add captions.json
            git commit -m "Mise à jour automatique des posts publiés"
            git push
          fi
